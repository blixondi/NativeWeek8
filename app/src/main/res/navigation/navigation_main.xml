<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_main"
    app:startDestination="@id/todoListFragment">

    <fragment
        android:id="@+id/todoListFragment"
        android:name="com.shem.todoapp160420033.view.TodoListFragment"
        android:label="To Do List"
        tools:layout="@layout/fragment_todo_list">
        <action
            android:id="@+id/actionCreateTodo"
            app:destination="@id/createTodoFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out" />
        <action
            android:id="@+id/actionEditTodoFragment"
            app:destination="@id/editTodoFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out" />
    </fragment>
    <fragment
        android:id="@+id/createTodoFragment"
        android:name="com.shem.todoapp160420033.view.CreateTodoFragment"
        android:label="Create New To Do"
        tools:layout="@layout/fragment_create_todo" />
    <fragment
        android:id="@+id/editTodoFragment"
        android:name="com.shem.todoapp160420033.view.EditTodoFragment"
        android:label="Edit To Do">
        <argument
            android:name="uuid"
            app:argType="integer" />
    </fragment>
</navigation>